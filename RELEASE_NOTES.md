# VoxFlow v2.1.0 — встроенный локальный ИИ, новый интерфейс, честное распознавание

Релиз поверх 2.0.20. Три темы: диктовка перестала терять слова и подсовывать
перевод; локальная нейросеть для обработки текста ставится из самого
приложения; интерфейс перерисован с нуля.

## Распознавание

**Слова больше не стираются.** Правило устных исправлений считало обычные
слова командами: «у меня нет времени» превращалось в «у времени», «погоди
меня» и «не забудь» теряли слово, «сделай текст короче» лишалось «короче».
Теперь одиночное «нет», «ой», «погоди», «подожди» — исправление только когда
оно стоит отдельной вставкой с паузами по обе стороны («завтра, нет,
послезавтра»); «точнее», «вернее», «отмена», «забудь» требуют паузы слева;
«короче» режется только как вводное в начале фразы. Якорная переигровка той же
позиции («на понедельник, ой, на вторник») работает как раньше. Режим
«агрессивные самоисправления» поведение не менял. Защита ИИ-рерайта от потери
слов больше не считает «нет» одноразовым словом.

**Перевод вместо речи.** В режиме «авто» whisper иногда решал, что русская речь
— английская, и вставлял английский перевод. Теперь результат GigaAM с
кириллицей всегда побеждает чисто латинский результат whisper того же аудио;
смешанная речь с английскими терминами («открой Settings в VS Code») сразу
принимается от GigaAM без медленного whisper-прохода; подсказка whisper для
auto стала двуязычной и начинается с русского.

## Встроенный локальный ИИ

Новый раздел «Локальный ИИ» в главном меню. Приложение само скачивает движок
llama.cpp (закреплённая сборка b10809) и GGUF-модель — Qwen3 1.7B, Qwen2.5 3B
Instruct, Qwen3 4B, Gemma 3 4B, Qwen3 8B — с проверкой контрольных сумм
(digest из GitHub Releases, sha256 из LFS-метаданных HuggingFace). Ряд
подбирается под память и ускоритель машины, как у каталога Ollama. Сервер
поднимается на loopback-порту 8772 и живёт вместе с приложением; наружу
уходят только загрузки. Бэкенд «Встроенный локальный ИИ» доступен и в
«Обработке текста»; Ollama, LM Studio и облачные ключи остались на месте.

## Графическое ускорение

Настройка «Диктовка → Графическое ускорение»: авто / вкл / выкл. Управляет
выбором CUDA-сборки whisper, живым стримом на Windows и выгрузкой слоёв
встроенной модели на видеокарту (Vulkan на Windows, Metal на Apple Silicon).
GigaAM и Parakeet по-прежнему считают на процессоре.

## Интерфейс

Полностью новая таблица стилей: графитовый пульт диктовки с индикатором уровня
по реальному сигналу микрофона, один сигнальный акцент, красный только для
записи; светлая и тёмная темы. Главный экран — «Диктовка»; карточка локального
ИИ и быстрые настройки справа. Разделы и разметка настроек сохранены.

## Проверка

- Rust: 364 unit-теста пройдено, 7 пропущено (нужны модели); clippy
  `-D warnings` чист по изменённым файлам; `cargo fmt --check` чист.
- Frontend: 33 теста, `tsc`, `vite build` пройдены; `check_versions.py --tag v2.1.0` чист.
- Сборка runtime llama.cpp и загрузка моделей проверены на уровне разбора
  манифестов (unit-тесты); сетевой прогон — на реальной машине с доступом к
  GitHub и HuggingFace.

## Артефакты

- `VoxFlow-Setup-2.1.0.exe`
- `SHA256SUMS-windows.txt`
- `VoxFlow-macOS-2.1.0-arm64-adhoc.dmg`
- `VoxFlow-macOS-2.1.0-arm64-adhoc.app.zip`
- `SHA256SUMS.txt`
- `release-manifest.txt`

# VoxFlow v2.0.20 — постобработка перестала удалять сказанное

Релиз поверх 2.0.19. Разбор начался с записи, где 21 секунда речи превратилась
в шесть слов «Смотри, у меня появляется текст. Он». GigaAM распознал всё
правильно; середину вырезала постобработка.

## «То есть» удаляло всё между двумя «у»

Правило устных исправлений («в пять, то есть в шесть») искало якорь — слово,
с которого начинается правая часть, — по всей левой части фразы. Во фразе
«…у голосового ввода такая проблема … то есть у меня появляется текст» якорем
стало «у» из начала предложения, и правило вырезало тридцать слов между ними.
«То есть я …» точно так же цеплялось за любое «я» раньше в предложении.

**Исправление.** Якорь обязан стоять в хвосте левой части: переигранный кусок
не длиннее четырёх слов и не длиннее самой замены. Для переформулировок
(«то есть», «в смысле») местоимения и союзы якорем не служат — это связка речи.
Повтор самого маркера («нет, нет, нет») исправлением не считается. Короткие
исправления («во вторник, то есть в среду», «нет, стоп, в шесть») режутся как
раньше; регрессионный тест — сама запись из отчёта, слово в слово.

## Слово, сказанное трижды, показывается трижды

Антизацикливание срезало три одинаковых слова или фразы подряд до одного —
и в плашке, и во вставке. Человек так говорит постоянно: «нет, нет, нет»,
«тест, тест, тест», трижды повторённая просьба.

**Исправление.** Петлёй декодера считаются четыре и более копий подряд; порог
общий для одиночных слов и n-грамм, поэтому плашка и вставка режут одинаково.
Заикание на служебных словах («я я я думаю») по-прежнему схлопывается.

## Английская речь видна в плашке

В режиме «авто» с GigaAM живое превью прятало любой кусок, где кириллицы
меньше латиницы: GigaAM v3 отдаёт английскую речь латиницей, и человек говорил
по-английски в пустую плашку, пока финал не дорисует whisper.

**Исправление.** Превью показывает всё, что распознал движок; бейдж языка
считается по скрипту показанного текста. Финальная вставка не изменилась:
нерусский результат GigaAM по-прежнему уточняется whisper-ом.

## Проверка

- Запись из отчёта прогнана через финальный локальный путь (`eval_wav_pipeline`):
  до исправления — «Смотри, у меня появляется текст. Он», после — полный текст.
- Rust: 359 unit-тестов пройдено, 7 пропущено (нужны локальные модели), clippy
  `-D warnings` чист, `cargo fmt --check` чист.
- Frontend: 33 теста и сборка пройдены, `check_versions.py --tag v2.0.20` чист.
- Windows x64 и macOS ARM64 собираются одним release workflow по тегу.

## Артефакты

- `VoxFlow-Setup-2.0.20.exe`
- `SHA256SUMS-windows.txt`
- `VoxFlow-macOS-2.0.20-arm64-adhoc.dmg`
- `VoxFlow-macOS-2.0.20-arm64-adhoc.app.zip`
- `SHA256SUMS.txt`
- `release-manifest.txt`
